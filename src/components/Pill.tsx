import { styled } from '@stitches/react';

const Pill = styled('code', {
	fontFamily: 'monospace',
	fontSize: '0.9em',
	backgroundColor: 'var(--color-code-bg)',
	padding: '0.1em 0.3em',
	borderRadius: '3px',
});

export default Pill;
